generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Test {
  id       Int    @id @default(autoincrement())
  email    String
  password String
}

model Feed {
  id         Int    @id @default(autoincrement())
  author_sub String
  title      String
  secret     String
}
